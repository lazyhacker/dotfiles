conky.config = {
-- Conky settings #
	background = true,
	update_interval = 60,
	double_buffer = true,
	no_buffers = true,
	imlib_cache_size = 10,

-- Window specifications #
	alignment = 'top_left',
	minimum_width = 500, minimum_height = 800,
	own_window = true,
	own_window_type = 'normal',-- other options are: override/dock/desktop/panel
	own_window_transparent = false,
	own_window_hints = 'undecorated,sticky,skip_taskbar,skip_pager,below',
	border_inner_margin = 0,
	border_outer_margin = 0,
	own_window_argb_visual = true,
	own_window_argb_value = 0,

-- Graphics settings #
	draw_shades = false,
	default_shade_color = '#AAAAAA',
	draw_outline = false,
	default_outline_color = '#AAAAAA',
	draw_borders = false,
	draw_graph_borders = false,

    default_color = 'yellow',  -- Set default text color to yellow
    use_xft = true,

    gap_x = -1070, gap_y = -450,  -- Adjust for spacing
    font = 'Terminus:size=14',  -- Change font style/size if necessary

};

conky.text = [[
${exec cat $HOME/dotfiles/vim.txt}
]];

